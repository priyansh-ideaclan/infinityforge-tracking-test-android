# Factory-wide R8/ProGuard rules. Keep this file minimal: prefer library-provided
# consumer-rules.pro (Room, Retrofit, Firebase, RevenueCat, Google Mobile Ads all ship
# their own) over rules duplicated here. Add app-specific rules only when a real
# release build surfaces a concrete R8 failure — do not pre-emptively broad-keep classes.
